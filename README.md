### `segment-ne`

Estimate piecewise-constant demographies using pairwise divergence likelihood.

#### Compiling

The program depends on the `xsf` library from the `scipy` ecosystem. To compile on an HPC cluster, I found it necessary to `module load llvm/15.0.3`
